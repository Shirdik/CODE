package Java.OTHER;

import java.util.HashMap;
import java.util.Map;

public class VowelSubString {

    public static int vowelSubs(String s) {
        int o = 0;
        int c, i, j = 0;
        Map<Character, Integer> m = new HashMap<>();
        // Map.of('a', 1, 'b', 1, 'c', 1, 'd', 1, 'e', 1);
        // while (j < s.length()) {

        // }
        m.computeIfAbsent('x', n -> 1 * 2);
        return o;

    }

    public static void main(String[] args) {

        System.out.println(vowelSubs(""));
    }
}
